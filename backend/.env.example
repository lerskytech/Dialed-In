# Dialed-In Lead Generation System - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Port for the backend server to run on
# Default: 3002 (to avoid conflicts with other services)
PORT=3002

# =============================================================================
# GOOGLE PLACES API CONFIGURATION
# =============================================================================

# Google Places API key - REQUIRED for lead generation
# Get your API key from: https://console.cloud.google.com/
# 
# Setup Steps:
# 1. Create/select a Google Cloud project
# 2. Enable "Places API (New)" in the API Library
# 3. Enable billing (required for Places API usage)
# 4. Create an API key in "APIs & Services" > "Credentials"
# 5. (Optional) Restrict the key to Places API only for security
#
# Pricing: ~$0.017 per lead (Text Search + Place Details)
# Example: 100 leads ≈ $1.70
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# =============================================================================
# AUTHENTICATION TOKENS
# =============================================================================

# Access tokens for multi-user authentication
# These tokens identify users and enable collaborative access
# 
# Token Mapping:
# - dialed-in-partner-access-2024 → Skyler (primary user)
# - dialed-in-business-partner-2024 → Eden (business partner)
#
# Users login with these tokens to access the system
# Each user can configure their own Google Places API key for separate billing
DIALED_IN_ACCESS_TOKEN_1=dialed-in-partner-access-2024
DIALED_IN_ACCESS_TOKEN_2=dialed-in-business-partner-2024

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Database configuration (currently using SQLite in-memory)
# For production, consider switching to persistent database:
# DATABASE_URL=sqlite:./leads.db
# or PostgreSQL: DATABASE_URL=postgresql://user:pass@localhost:5432/dialed_in

# Rate limiting (requests per minute per user)
# RATE_LIMIT_PER_MINUTE=60

# Enable debug logging
# DEBUG=true

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# For Netlify deployment, set these environment variables in your Netlify dashboard:
# - GOOGLE_PLACES_API_KEY
# - DIALED_IN_ACCESS_TOKEN_1
# - DIALED_IN_ACCESS_TOKEN_2
#
# The PORT variable is automatically set by Netlify Functions

# =============================================================================
# SECURITY NOTES
# =============================================================================

# NEVER commit your actual .env file to version control
# Add .env to your .gitignore file
# 
# For production:
# - Use environment-specific API keys
# - Consider rotating access tokens periodically
# - Monitor API usage and set quotas in Google Cloud Console
# - Enable API key restrictions to prevent unauthorized usage
